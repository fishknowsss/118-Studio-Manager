export const inputClass = 'w-full px-3 py-2 text-sm border border-border rounded-md bg-white text-text-primary placeholder:text-text-muted focus:outline-none focus:ring-2 focus:ring-primary/30 focus:border-primary transition-colors'

export const selectClass = inputClass

export const textareaClass = `${inputClass} resize-none`
